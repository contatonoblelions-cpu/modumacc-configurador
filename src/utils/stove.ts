/**
 * Fogão -- elemento só visual/referência (2026-08-24), não é produto
 * vendável (ver `StoveFixture` em `types/composition.ts`), mesma ideia da
 * geladeira. Faixas reais de fogão de piso (fonte do cliente): 4 bocas
 * ~48-56cm larg / 86-95cm alt; 5 bocas ~75-80cm; 6 bocas ~76-90cm. Padrão:
 * 4 bocas típico (52 x 90cm).
 */
export const STOVE_WIDTH_CM = 52;
export const STOVE_HEIGHT_CM = 90;
