/**
 * ARQUIVO DESATIVADO — o sistema de fileiras (superior/inferior/torre/geral)
 * foi removido em favor de posicionamento 2D totalmente livre (X e Y), sem
 * nenhuma categoria fixa (ver `utils/placement.ts` e `components/BuildCanvas.tsx`).
 * Este arquivo não é mais importado por nada; mantido vazio só pra não deixar
 * um `.ts` quebrado no histórico do repositório.
 */
export {};
